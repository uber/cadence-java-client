rootProject.name = 'cadence-client'

if(JavaVersion.current() != JavaVersion.VERSION_11){
    // specifically:
    // - known broken on 13, error-prone throws an exception
    throw new GradleException("This build must be run with Java 11")
}